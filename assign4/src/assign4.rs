/*fn main() {
	let x = read_from();
	println!("{:?}", x);
	
}

fn read_from() -> Vec<String>{
	use std::io;
	use std::io::prelude::*;
	let mut vec = Vec::new();	
	let stdin = io::stdin();
    	for line in stdin.lock().lines() {
    		for word in line.unwrap().split_whitespace().collect::<Vec<String>>(){
        		vec.push(word);
        	}
    	}
	println!("Vector: {:?}", vec);
	vec
}

fn main() {
	let x = read_from();
	println!("{:?}", x);
	
}

fn read_from() -> Vec<String>{
	use std::io;
	use std::io::prelude::*;
	let mut vec = Vec::new();
	let stdin = io::stdin();
   	for line in stdin.lock().lines() {
		vec.push(line.unwrap().to_string());
   	}
	println!("Vector: {:?}", vec);
	vec
}

fn match (a_vec : Vec<String>) {

	for string in a_vec {
		
	}

}*/
